let container = document.getElementById('container');

const inc = () => {
    container.innerHTML = +container.innerHTML + 1
};

const dec = () => {
    container.innerHTML = +container.innerHTML - 1
};